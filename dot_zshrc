ZSH_RC_DIR="$HOME/.zsh"

zsh_files=(
  env.zsh
  options.zsh
  plugins.zsh
  init.zsh
  completion.zsh
  history.zsh
  theme.zsh
  aliases.zsh
)

for file in "${zsh_files[@]}"; do
  full_path="$ZSH_RC_DIR/$file"
  [[ -r "$full_path" ]] && source "$full_path"
done
